# ANDROID BUILD FIX: Optimized Gradle configuration for stable builds
org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G -XX:+UseG1GC
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.configureondemand=false
org.gradle.caching=false

# Flutter Android configuration
android.useAndroidX=true
android.enableJetifier=false
android.compileSdkVersion=34
android.buildToolsVersion=34.0.0
android.targetSdkVersion=34

# ANDROID BUILD FIX: Critical Kotlin configuration  
kotlin.code.style=official
kotlin.incremental=false
kotlin.incremental.android=false
kotlin.daemon.jvmargs=-Xmx2G

# ANDROID BUILD FIX: Essential Gradle optimizations
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.configureondemand=false
org.gradle.caching=false

# ANDROID BUILD FIX: R8 and resource optimizations
android.enableR8=true
android.enableR8.fullMode=false
android.enableResourceOptimizations=true
android.enableBuildCache=false

# ANDROID BUILD FIX: Dependency resolution improvements
android.suppressUnsupportedCompileSdk=34,35
android.overrideVersionCheck=true

# ANDROID BUILD FIX: Build performance tuning
android.enableJetpack=true
android.nonTransitiveRClass=true
android.nonFinalResIds=false

# Flutter specific optimizations
flutter.minifyEnabled=false
flutter.useProguard=false

# ANDROID BUILD FIX: Disable Gradle configuration cache that can cause issues
org.gradle.configuration-cache=false